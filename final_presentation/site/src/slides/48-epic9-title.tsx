import type { SlideProps } from './types';
import { QuoteBreakout } from '../components/QuoteBreakout';

/**
 * Slide 48 — Epic 9: View Messages title.
 */
export function Slide48Epic9Title(_props: SlideProps) {
  return (
    <QuoteBreakout
      kicker="EPIC 9 · DEEP DIVE"
      quote="The one epic we explore story by story."
      subtitle="View Messages — Apr 2 – Apr 8"
    />
  );
}
