import type { SlideProps } from './types';
import { SectionTitle } from '../components/SectionTitle';

/**
 * Slide 38 — Milestone 7: Browse & Apply title card.
 */
export function Slide38M7Title(_props: SlideProps) {
  return (
    <SectionTitle
      kicker="MILESTONE 7"
      title="Browse & Apply"
      dateRange="Mar 25"
      epicNumber={7}
      act="PART B · BUILDING INCOLLEGE"
    />
  );
}
